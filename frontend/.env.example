# Deployed frontend builds must set this to the FastAPI service origin,
# for example: https://quantitative-sentiment-analysis.onrender.com
# Local Vite development can leave it unset to use the /api proxy.
# Browser auth uses HttpOnly cookies, so backend CORS must allow this origin.
VITE_API_BASE_URL=
