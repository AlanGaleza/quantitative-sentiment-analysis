services:
  - type: web
    runtime: python
    name: quantitative-sentiment-analysis
    region: frankfurt
    plan: free
    buildCommand: uv sync --locked
    startCommand: uv run uvicorn quantitative_sentiment_analysis.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeployTrigger: commit
